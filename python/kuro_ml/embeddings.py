"""Embedding generation for semantic search."""

# Placeholder - to be implemented


def generate_embedding(text: str) -> list[float]:
    """
    Generate text embedding for semantic search.

    Args:
        text: Text to embed

    Returns:
        Embedding vector as list of floats
    """
    raise NotImplementedError("Embedding generation not yet implemented")


def generate_visual_embedding(image_path: str) -> list[float]:
    """
    Generate visual embedding from image.

    Args:
        image_path: Path to image file

    Returns:
        Embedding vector as list of floats
    """
    raise NotImplementedError("Visual embedding not yet implemented")
